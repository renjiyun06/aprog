{
  "name": "@aprog/sandbox",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "aprog 沙箱层（A 平面）：精瘦运行时 provider 库（create/destroy，厂商隔离，只认不透明 ImageRef）。打镜像不在此包——镜像是仓库顶层 images/<名>/<版本>/bake.ts，由 tools/bake（aprog-bake）跑；control-plane 运行时只消费 ImageRef。",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aprog/log": "workspace:*",
    "wuying-agentbay-sdk": "^0.22.0"
  }
}
