{
  "name": "@aprog/driver",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "engine driver —— 常驻沙箱，把底层 harness(Claude Agent SDK / Codex) 翻译成 @aprog/protocol 事件流。",
  "main": "./src/index.ts",
  "scripts": {
    "build": "bun build --target=node ./src/main.ts --outfile dist/driver.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.185",
    "@aprog/protocol": "workspace:*"
  }
}
